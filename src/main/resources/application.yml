spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: courses_db

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG